export enum UserStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    DECEASED = "deceased",
    TERMINATED = "terminated"
}

export enum LoginType {
    GOOGLE = "google",
    NORMAL = "normal"
}

